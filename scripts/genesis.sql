CREATE TABLE books (
    id INTEGER PRIMARY KEY,
    title TEXT NOT NULL
);

-- Later on, we will add columns for page number, completed, in progress, started, timestamps, chapter, favorite, things like that.
